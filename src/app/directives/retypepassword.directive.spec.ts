import { RetypepasswordDirective } from './retypepassword.directive';

describe('RetypepasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RetypepasswordDirective();
    expect(directive).toBeTruthy();
  });
});
